import 'package:flutter/material.dart';

class ApproveRejectRequestScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Approve/Reject Request'),
      ),
      body: Center(
        child: Text('Approve/Reject Request Screen'),
      ),
    );
  }
}
